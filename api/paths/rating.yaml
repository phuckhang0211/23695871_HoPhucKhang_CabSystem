trips~1{tripId}~1rating:
  post:
    tags:
      - Rating
    summary: Đánh giá chuyến xe
    operationId: createTripRating
    x-fr:
      - FR19

    security:
      - bearerAuth: []

    parameters:
      - $ref: "../parameters.yaml#/TripId"

    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "../schemas/rating.yaml#/CreateRatingRequest"

    responses:
      "201":
        description: Đánh giá thành công
        content:
          application/json:
            schema:
              $ref: "../schemas/rating.yaml#/Rating"

      "400":
        $ref: "../responses.yaml#/BadRequest"

      "401":
        $ref: "../responses.yaml#/Unauthorized"

      "404":
        $ref: "../responses.yaml#/NotFound"

      "409":
        $ref: "../responses.yaml#/Conflict"

  get:
    tags:
      - Rating
    summary: Xem đánh giá của chuyến xe
    operationId: getTripRating
    x-fr:
      - FR19

    security:
      - bearerAuth: []

    parameters:
      - $ref: "../parameters.yaml#/TripId"

    responses:
      "200":
        description: Đánh giá của chuyến xe
        content:
          application/json:
            schema:
              $ref: "../schemas/rating.yaml#/Rating"

      "401":
        $ref: "../responses.yaml#/Unauthorized"

      "404":
        $ref: "../responses.yaml#/NotFound"
